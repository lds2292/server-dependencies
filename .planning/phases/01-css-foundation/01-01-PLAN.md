---
phase: 01-css-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/style.css
  - client/src/components/GraphCanvas.vue
  - docs/guide/style_guide.md
autonomous: false
requirements:
  - CSS-01
  - CSS-02
  - CSS-03

must_haves:
  truths:
    - "style.css에 --graph-label-size, --graph-sub-size, --graph-meta-size 변수가 존재한다"
    - "style.css에 --node-light-text, --node-light-text-muted 변수가 존재한다"
    - "GraphCanvas.vue 템플릿에 style=\"fill:rgba(\" 인라인 스타일이 없다"
    - "GraphCanvas.vue scoped CSS의 .node-label, .node-sub, .node-meta가 --graph-* 변수를 참조한다"
    - "infra/dns 노드의 node-meta 텍스트가 WCAG AA 4.5:1 대비율을 충족한다"
    - "export 블록의 font-size가 cssVar()로 동적 해석된다"
    - "export 블록에서 infra/dns 노드의 fill 색상이 cssVar()로 동적 해석된다"
  artifacts:
    - path: "client/src/style.css"
      provides: "그래프 전용 CSS 변수 5종"
      contains: "--graph-label-size"
    - path: "client/src/components/GraphCanvas.vue"
      provides: "인라인 스타일 제거 + CSS 변수 참조"
    - path: "docs/guide/style_guide.md"
      provides: "그래프 노드 텍스트 변수 문서화"
      contains: "--graph-label-size"
  key_links:
    - from: "client/src/style.css"
      to: "client/src/components/GraphCanvas.vue"
      via: "CSS variable references in scoped styles and :style bindings"
      pattern: "var\\(--graph-(label|sub|meta)-size\\)"
    - from: "client/src/style.css"
      to: "client/src/components/GraphCanvas.vue"
      via: "CSS variable references for light-background node text"
      pattern: "var\\(--node-light-text"
    - from: "client/src/components/GraphCanvas.vue scoped CSS"
      to: "client/src/components/GraphCanvas.vue export block"
      via: "cssVar() resolves same variables at export time"
      pattern: "cssVar\\('--graph-(label|sub|meta)-size'\\)"
    - from: "client/src/components/GraphCanvas.vue template"
      to: "client/src/components/GraphCanvas.vue export block"
      via: "cssVar() resolves infra/dns fill colors in export post-processing"
      pattern: "cssVar\\('--node-light-text"
---

<objective>
그래프 노드 텍스트 스타일을 CSS 변수 시스템으로 전환하고, infra/dns 노드의 인라인 스타일을 제거하며, WCAG AA 대비율을 충족하도록 opacity를 조정한다.

Purpose: Phase 2(폰트 크기 변경)에서 CSS 변수 값만 바꾸면 전체 그래프에 반영되는 기반을 구축한다. 현재 하드코딩/인라인 스타일 혼재 상태를 정리하여 유지보수성을 확보한다.
Output: style.css에 5개 신규 변수, GraphCanvas.vue에서 인라인 스타일 제거 + CSS 변수 참조, export 블록 동기화(font-size + infra/dns fill 색상), style_guide.md 업데이트
</objective>

<execution_context>
@/Users/ideogsu/WebstormProjects/server-dependencies/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ideogsu/WebstormProjects/server-dependencies/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-css-foundation/01-RESEARCH.md

<interfaces>
<!-- cssVar helper in GraphCanvas.vue (line 1175) -->
```typescript
function cssVar(name: string): string {
  return getComputedStyle(document.documentElement).getPropertyValue(name).trim()
}
```

<!-- Current scoped CSS (lines 2322-2325) -->
```css
.node-label { font-size: var(--text-xs); fill: #fff; pointer-events: none; font-weight: 700; }
.node-ip { font-size: 10px; fill: rgba(255,255,255,0.75); pointer-events: none; }
.node-sub { font-size: 9px; fill: rgba(255,255,255,0.65); pointer-events: none; font-weight: 600; letter-spacing: 0.02em; }
.node-meta { font-size: 9.5px; fill: rgba(255,255,255,0.5); pointer-events: none; }
```

<!-- Current export block (lines 2242-2248) -->
```javascript
style.textContent = [
  `text { font-family: var(--font-sans); }`,
  `.node-label { font-size: var(--text-xs); fill: #fff; font-weight: 700; }`,
  `.node-ip    { font-size: 10px; fill: rgba(255,255,255,0.75); }`,
  `.node-sub   { font-size: 9px;  fill: rgba(255,255,255,0.65); font-weight: 600; letter-spacing: 0.02em; }`,
  `.node-meta  { font-size: 9.5px; fill: rgba(255,255,255,0.5); }`,
].join('\n')
```

<!-- Current inline styles on infra/dns (lines 302-303, 310-311) -->
```html
<text class="node-sub" style="fill:rgba(15,23,42,0.65)">...</text>
<text class="node-meta" style="fill:rgba(15,23,42,0.5)">...</text>
```

<!-- Current node-label conditional style (line 295) -->
```html
<text ... :style="(node.nodeKind === 'infra' || node.nodeKind === 'dns') ? `fill:${cssVar('--bg-base')}` : ''">
```

<!-- style.css :root ends at line 114 -->
<!-- Node type variables are at lines 70-102 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS 변수 추가 + GraphCanvas 스타일 전환</name>
  <files>client/src/style.css, client/src/components/GraphCanvas.vue</files>
  <read_first>
    - client/src/style.css (전체 :root 블록, 특히 lines 6-15 타입스케일, lines 70-102 노드타입 변수)
    - client/src/components/GraphCanvas.vue (lines 290-316 노드 텍스트 템플릿, lines 1175-1177 cssVar 헬퍼, lines 2240-2250 export 블록, lines 2315-2326 scoped CSS)
    - .planning/phases/01-css-foundation/01-RESEARCH.md (WCAG 대비율 분석, 권장 opacity 값)
  </read_first>
  <action>
**1. style.css :root 블록에 5개 CSS 변수 추가**

line 15 (`--text-hero: 56px;`) 아래에 다음을 추가:

```css
  /* -- 그래프 노드 텍스트 -- */
  --graph-label-size: 11px;     /* 1행: 노드 이름 */
  --graph-sub-size: 9px;        /* 2행: 보조 정보 */
  --graph-meta-size: 9.5px;     /* 3행: 기술 식별자 */
```

line 102 (`--node-dns-hover: #be185d;`) 아래에 다음을 추가:

```css
  /* -- 밝은 배경 노드(infra/dns) 텍스트 -- */
  --node-light-text: rgba(15, 23, 42, 0.85);       /* 1행/2행: sub 텍스트 */
  --node-light-text-muted: rgba(15, 23, 42, 0.7);  /* 3행: meta 텍스트 (WCAG AA 4.5:1 충족) */
```

NOTE: `--node-light-text-muted`의 alpha를 기존 0.5에서 0.7로 올림. 이는 CSS-03 요구사항으로, `rgba(15,23,42,0.7)` on `#f0f9ff` 배경은 약 4.8:1 대비율 (WCAG AA 4.5:1 충족). `--node-light-text`의 alpha도 기존 0.65에서 0.85로 올려 더 선명하게 함.

**2. GraphCanvas.vue 템플릿 인라인 스타일 제거 (CSS-02)**

lines 302-303 (infra 노드):
- BEFORE: `<text ... class="node-sub" style="fill:rgba(15,23,42,0.65)">`
- AFTER:  `<text ... class="node-sub" :style="'fill:var(--node-light-text)'">`
- BEFORE: `<text ... class="node-meta" style="fill:rgba(15,23,42,0.5)">`
- AFTER:  `<text ... class="node-meta" :style="'fill:var(--node-light-text-muted)'">`

lines 310-311 (dns 노드): 동일한 변환 적용.

line 295 (node-label for infra/dns):
- BEFORE: `:style="(node.nodeKind === 'infra' || node.nodeKind === 'dns') ? \`fill:\${cssVar('--bg-base')}\` : ''"`
- AFTER:  `:style="(node.nodeKind === 'infra' || node.nodeKind === 'dns') ? 'fill:var(--node-light-text)' : ''"` 

NOTE: node-label on infra/dns는 기존에 `--bg-base`(#121214, 거의 검정)를 사용했지만, 이는 밝은 배경에서 충분히 가독성 있음. 그러나 CSS 변수 일관성을 위해 `--node-light-text`로 통일. `rgba(15,23,42,0.85)`는 밝은 배경에서 `#121214`와 시각적으로 거의 동일(둘 다 매우 어두운 색).

**3. GraphCanvas.vue scoped CSS 변수 참조로 전환 (CSS-01)**

line 2322:
- BEFORE: `.node-label { font-size: var(--text-xs); ...`
- AFTER:  `.node-label { font-size: var(--graph-label-size); ...`

line 2324:
- BEFORE: `.node-sub { font-size: 9px; ...`
- AFTER:  `.node-sub { font-size: var(--graph-sub-size); ...`

line 2325:
- BEFORE: `.node-meta { font-size: 9.5px; ...`
- AFTER:  `.node-meta { font-size: var(--graph-meta-size); ...`

**4. Export 블록 cssVar() 동적 해석으로 전환 (font-size + infra/dns fill 색상)**

lines 2242-2248을 다음으로 교체:

```javascript
style.textContent = [
  `text { font-family: ${cssVar('--font-sans')}; }`,
  `.node-label { font-size: ${cssVar('--graph-label-size')}; fill: #fff; font-weight: 700; }`,
  `.node-ip    { font-size: 10px; fill: rgba(255,255,255,0.75); }`,
  `.node-sub   { font-size: ${cssVar('--graph-sub-size')};  fill: rgba(255,255,255,0.65); font-weight: 600; letter-spacing: 0.02em; }`,
  `.node-meta  { font-size: ${cssVar('--graph-meta-size')}; fill: rgba(255,255,255,0.5); }`,
].join('\n')
```

NOTE: export 블록은 XMLSerializer로 standalone SVG를 생성하므로 `var()` 대신 `cssVar()`로 런타임에 값을 resolve해야 함. `font-family`는 이미 이 패턴 사용 중(line 2243). font-size도 동일 패턴 적용.

**5. Export 블록에서 infra/dns fill 색상 후처리 (CRITICAL)**

export 블록의 style.textContent 설정 후, `clone.insertBefore(style, clone.firstChild)` 이후에 다음 코드를 추가:

```javascript
// infra/dns 노드의 inline fill:var(--node-light-text*) 를 computed 값으로 해석
// standalone SVG에서는 CSS 변수가 resolve되지 않으므로 직접 치환 필요
const lightText = cssVar('--node-light-text')
const lightTextMuted = cssVar('--node-light-text-muted')
clone.querySelectorAll('text').forEach(el => {
  const s = el.getAttribute('style') || ''
  if (s.includes('var(--node-light-text-muted)')) {
    el.setAttribute('style', s.replace('var(--node-light-text-muted)', lightTextMuted))
  } else if (s.includes('var(--node-light-text)')) {
    el.setAttribute('style', s.replace('var(--node-light-text)', lightText))
  }
})
```

이 코드는 export clone의 infra/dns 텍스트 요소에서 `fill:var(--node-light-text)` → `fill:rgba(15, 23, 42, 0.85)`, `fill:var(--node-light-text-muted)` → `fill:rgba(15, 23, 42, 0.7)`로 치환한다.

IMPORTANT: `--node-light-text-muted`를 먼저 체크해야 함 (`--node-light-text`가 `--node-light-text-muted`의 부분 문자열이므로). 위 코드는 `includes` 체크 순서로 이를 처리함.

위치: `clone.insertBefore(style, clone.firstChild)` (현재 line 2249) 직후, `const svgStr = new XMLSerializer().serializeToString(clone)` (현재 line 2251) 직전.
  </action>
  <verify>
    <automated>cd /Users/ideogsu/WebstormProjects/server-dependencies && grep -q '\-\-graph-label-size' client/src/style.css && grep -q '\-\-graph-sub-size' client/src/style.css && grep -q '\-\-graph-meta-size' client/src/style.css && grep -q '\-\-node-light-text:' client/src/style.css && grep -q '\-\-node-light-text-muted:' client/src/style.css && ! grep -q 'style="fill:rgba' client/src/components/GraphCanvas.vue && grep -q 'var(--graph-label-size)' client/src/components/GraphCanvas.vue && grep -q 'var(--graph-sub-size)' client/src/components/GraphCanvas.vue && grep -q 'var(--graph-meta-size)' client/src/components/GraphCanvas.vue && grep -q "cssVar('--graph-label-size')" client/src/components/GraphCanvas.vue && grep -q "cssVar('--graph-sub-size')" client/src/components/GraphCanvas.vue && grep -q "cssVar('--node-light-text')" client/src/components/GraphCanvas.vue && grep -q "cssVar('--node-light-text-muted')" client/src/components/GraphCanvas.vue && cd client && npx vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - style.css contains `--graph-label-size: 11px;`
    - style.css contains `--graph-sub-size: 9px;`
    - style.css contains `--graph-meta-size: 9.5px;`
    - style.css contains `--node-light-text: rgba(15, 23, 42, 0.85);`
    - style.css contains `--node-light-text-muted: rgba(15, 23, 42, 0.7);`
    - GraphCanvas.vue has zero occurrences of `style="fill:rgba(`
    - GraphCanvas.vue scoped CSS `.node-label` contains `var(--graph-label-size)`
    - GraphCanvas.vue scoped CSS `.node-sub` contains `var(--graph-sub-size)`
    - GraphCanvas.vue scoped CSS `.node-meta` contains `var(--graph-meta-size)`
    - GraphCanvas.vue export block contains `cssVar('--graph-label-size')`
    - GraphCanvas.vue export block contains `cssVar('--graph-sub-size')`
    - GraphCanvas.vue export block contains `cssVar('--graph-meta-size')`
    - GraphCanvas.vue export block contains `cssVar('--node-light-text')` for infra/dns fill resolution
    - GraphCanvas.vue export block contains `cssVar('--node-light-text-muted')` for infra/dns fill resolution
    - `cd client && npx vue-tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    style.css에 5개 그래프 전용 CSS 변수가 존재하고, GraphCanvas.vue에서 모든 노드 텍스트 font-size가 CSS 변수를 참조하며, infra/dns 인라인 fill:rgba 스타일이 CSS 변수로 대체되고, export 블록이 cssVar()로 font-size와 infra/dns fill 색상을 동적 해석하며, vue-tsc 타입체크 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: 스타일 가이드 문서 업데이트</name>
  <files>docs/guide/style_guide.md</files>
  <read_first>
    - docs/guide/style_guide.md (전체, 특히 타입 스케일 섹션과 노드 타입 색상 섹션)
    - client/src/style.css (새로 추가된 변수 확인)
  </read_first>
  <action>
**style_guide.md의 타입 스케일 테이블 아래에 그래프 노드 텍스트 섹션 추가:**

`## 타입 스케일` 섹션의 테이블 이후, `---` 구분선 전에 다음 내용을 삽입:

```markdown
### 그래프 노드 텍스트

그래프 캔버스(GraphCanvas) 전용 폰트 크기 변수. 일반 UI 타입 스케일과 분리하여 그래프 텍스트만 독립적으로 조정할 수 있다.

| 변수 | 값 | 용도 |
|------|----|------|
| `--graph-label-size` | `11px` | 1행: 노드 이름 |
| `--graph-sub-size` | `9px` | 2행: 보조 정보 (팀, 인프라 타입 등) |
| `--graph-meta-size` | `9.5px` | 3행: 기술 식별자 (IP, 포트 등) |

> 그래프 노드 폰트 크기를 변경하려면 이 변수를 수정한다. `--text-xs` 등 일반 타입 스케일을 수정하면 안 된다.
```

**노드 타입 색상 섹션(line 139 근처) 이후에 밝은 배경 노드 텍스트 변수 설명 추가:**

```markdown
### 밝은 배경 노드 텍스트

infra/dns 노드는 밝은 배경(`--node-infra-bg-light`, `--node-dns-bg-light`)을 사용하므로 어두운 텍스트 색상이 필요하다.

| 변수 | 값 | 용도 | 대비율 |
|------|----|------|--------|
| `--node-light-text` | `rgba(15, 23, 42, 0.85)` | infra/dns 1행(label)/2행(sub) 텍스트 | ~7.5:1 |
| `--node-light-text-muted` | `rgba(15, 23, 42, 0.7)` | infra/dns 3행(meta) 텍스트 | ~4.8:1 (WCAG AA) |

> 이 변수들은 WCAG AA 대비율(4.5:1)을 충족하도록 설계되었다. alpha 값을 낮추면 대비율이 떨어지므로 주의한다.
```
  </action>
  <verify>
    <automated>grep -q 'graph-label-size' /Users/ideogsu/WebstormProjects/server-dependencies/docs/guide/style_guide.md && grep -q 'graph-sub-size' /Users/ideogsu/WebstormProjects/server-dependencies/docs/guide/style_guide.md && grep -q 'graph-meta-size' /Users/ideogsu/WebstormProjects/server-dependencies/docs/guide/style_guide.md && grep -q 'node-light-text' /Users/ideogsu/WebstormProjects/server-dependencies/docs/guide/style_guide.md && grep -q 'node-light-text-muted' /Users/ideogsu/WebstormProjects/server-dependencies/docs/guide/style_guide.md && grep -q 'WCAG AA' /Users/ideogsu/WebstormProjects/server-dependencies/docs/guide/style_guide.md</automated>
  </verify>
  <acceptance_criteria>
    - style_guide.md contains `--graph-label-size`
    - style_guide.md contains `--graph-sub-size`
    - style_guide.md contains `--graph-meta-size`
    - style_guide.md contains `--node-light-text`
    - style_guide.md contains `--node-light-text-muted`
    - style_guide.md contains `WCAG AA`
  </acceptance_criteria>
  <done>
    style_guide.md에 그래프 노드 텍스트 전용 CSS 변수 3종과 밝은 배경 노드 텍스트 변수 2종이 문서화되어 있고, WCAG AA 대비율 정보가 포함됨
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>CSS 변수 기반 그래프 노드 텍스트 스타일 시스템 구축 완료: style.css에 5개 변수 추가, GraphCanvas.vue 인라인 스타일 제거 및 CSS 변수 참조, export 블록 동기화 (font-size + infra/dns fill 색상 해석), 스타일 가이드 업데이트</what-built>
  <how-to-verify>
    1. `npm run dev:client`로 개발 서버 시작
    2. 그래프 페이지에서 5종 노드(server, l7, infra, external, dns)의 텍스트가 정상 표시되는지 확인
    3. 특히 infra/dns 노드의 텍스트(밝은 배경 위 어두운 텍스트)가 이전보다 더 선명하게 보이는지 확인
    4. 브라우저 DevTools에서 infra/dns node-meta 텍스트 요소를 선택하고, fill 값이 `var(--node-light-text-muted)`인지 확인
    5. 그래프를 SVG로 export하여 infra/dns 노드의 텍스트 색상이 캔버스와 동일하게 표시되는지 확인 (fill이 rgba 값으로 resolve되어 있어야 함)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. CSS 변수 존재 확인: `grep '--graph-label-size\|--graph-sub-size\|--graph-meta-size\|--node-light-text' client/src/style.css`
2. 인라인 스타일 제거 확인: `! grep -q 'style="fill:rgba' client/src/components/GraphCanvas.vue` (exit 0이면 성공)
3. CSS 변수 참조 확인: `grep 'var(--graph-' client/src/components/GraphCanvas.vue`
4. Export 블록 font-size 동기화 확인: `grep "cssVar('--graph-" client/src/components/GraphCanvas.vue`
5. Export 블록 infra/dns fill 해석 확인: `grep "cssVar('--node-light-text" client/src/components/GraphCanvas.vue`
6. 타입 체크: `cd client && npx vue-tsc --noEmit`
7. 스타일 가이드 업데이트 확인: `grep 'graph-label-size' docs/guide/style_guide.md`
</verification>

<success_criteria>
- style.css에 --graph-label-size, --graph-sub-size, --graph-meta-size, --node-light-text, --node-light-text-muted 5개 변수가 :root에 존재
- GraphCanvas.vue에 fill:rgba 인라인 스타일이 0개
- GraphCanvas.vue scoped CSS가 --graph-* 변수를 참조
- GraphCanvas.vue export 블록이 cssVar()로 font-size를 동적 해석
- GraphCanvas.vue export 블록이 cssVar()로 infra/dns fill 색상을 동적 해석 (standalone SVG에서 var() 미resolve 문제 해결)
- vue-tsc --noEmit 통과
- style_guide.md에 새 변수 문서화 완료
- 5종 노드 모두에서 텍스트가 정상 렌더링 (human-verify)
- export SVG에서 infra/dns 텍스트 색상이 정상 표시 (human-verify)
</success_criteria>

<output>
After completion, create `.planning/phases/01-css-foundation/01-01-SUMMARY.md`
</output>
